from django.apps import AppConfig


class SocialConfig(AppConfig):
    """Django app config for a 'social' application."""
    
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'social'
